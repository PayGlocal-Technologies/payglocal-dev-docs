<Warning>Always confirm payment or onboarding status server-side via the respective Get Status API. Browser redirects, postMessage events, and callback URLs can fail silently or be intercepted — never rely on them as the sole source of truth.</Warning>
